apiVersion: apps/v1
kind: Deployment
metadata:
  name: management
  namespace: tonghai-dev
  labels:
    app: management
spec:
  replicas: 1
  selector:
    matchLabels:
      app: management
  template:
    metadata:
      labels:
        app: management
    spec:
      containers:
        - name: management
          image: thny-management:local
          imagePullPolicy: Never
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"

---
apiVersion: v1
kind: Service
metadata:
  name: management
  namespace: tonghai-dev
spec:
  type: NodePort
  selector:
    app: management
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
